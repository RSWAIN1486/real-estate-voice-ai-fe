# FastAPI and related dependencies
fastapi>=0.100.0
uvicorn==0.17.6
# gunicorn==20.1.0 # Often used for production, but confirm if your Render setup uses it directly
# pydantic==1.9.1
# starlette>=0.27.0,<0.30.0

# HTTP clients
httpx==0.23.0

# Authentication and security
# python-jose==3.3.0
# passlib==1.7.4
# bcrypt>=4.0.1,<5.0.0
# python-multipart==0.0.5 # For file uploads, if not used

# Environment and configuration
python-dotenv==0.20.0
# pyyaml>=6.0,<7.0.0

# Logging
coloredlogs>=15.0.1,<16.0.0

# Utilities
# email-validator==1.2.1
# Jinja2>=3.1.2,<4.0.0 # For HTML templating, if not used

# Data processing
# aiofiles==0.8.0 # For async file operations, if not used
# orjson>=3.8.10,<4.0.0 # FastAPI might use it if available, but stdlib json is also used
# pydantic-core>=2.10.0,<3.0.0
requests==2.28.0

# cryptography==37.0.2 # Often a sub-dependency, review if direct dependencies needing it were removed
# Authlib==1.0.1
# stripe==3.5.0
urllib3==1.26.9 # Sub-dependency of requests/httpx
# itsdangerous==2.1.2
# numpy==1.23.0
# pandas==2.0.0
openai>=1.0.0
# backoff==2.2.1

# Testing - should not be in production requirements
# pytest==7.1.2
# pytest-asyncio==0.18.3